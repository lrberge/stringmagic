


reference:
  - title: String interpolation and operations
    desc: Tools for string interpolation. Functions starting with `.` don't have error-handling. `dsb` interpolates with `.[]`.
    contents:
      - smagick
      - matches("sma|dsb")
  - title: Other string operations
    desc: Pattern detection, operations chaining, string cleaning and more
    contents:
      - matches("^str_")
      - _index_

