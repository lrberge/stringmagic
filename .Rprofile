


# rename_src = function(){
#   if(file.exists("_src")) file.rename("_src", "src")
# }

# change_src = function(){
#   if(file.exists("src")) file.rename("src", "_src")
# }

# # m = Sys.getenv()
# # mm = paste0(names(m), ": ", m)
# # writeLines(mm, "sysenv_bis.txt")

# if(identical(Sys.getenv("RSTUDIO"), "1") && identical(Sys.getenv("R_MAKEVARS_USER"), "")){
#   # we don't want it to proc in vs code
#   # the second condition is to ensure it's no within devtools
#   change_src()
# }

# open_all = function(){

#   # Opening R files

#   R_not_open = c("R/test_funs.R", "R/RcppExports.R")

#   R_files = list.files("R/", full.names = TRUE)

#   for(f in R_files){
#     if(f %in% R_not_open) next
#     eval(str2lang(paste0("rstudioapi::navigateToFile('", f, "', moveCursor = FALSE)")))
#   }

# }

